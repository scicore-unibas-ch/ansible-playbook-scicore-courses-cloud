# Playbooks

## Main playbook (entry point)

Main playbook is `site.yml`. You should use this one by default do execute a complete deploy/config


## Extra playbooks

Add to this folder any extra playbook required to run custom tasks e.g. `playbook-renew-ssl-certs.yml`
